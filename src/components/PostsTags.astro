---
import { getUniqueTags } from '../utils/tags';
import Tag from './Tag.astro';

const { posts } = Astro.props;
const tags = getUniqueTags(posts);
---

<div class="flex flex-wrap mx-0 my-auto">
  {tags.map((tag) => <Tag name={tag} />)}
</div>
