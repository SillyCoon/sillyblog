---
import Layout from '../../layouts/Layout.astro';
import PostsTags from '../../components/PostsTags.astro';
const posts = await Astro.glob('../posts/*.md');
---

<Layout title="Tags">
  <PostsTags tags={posts.flatMap((p) => p.frontmatter.tags)} />
</Layout>
